# Contributing

## Development

```bash
pip install -e ".[dev]"
python -m pytest -q
```

## Pull Requests

- Keep changes small and focused.
- Add/adjust tests for new behavior.
- Artifacts/JSON contracts must be **additive only** (see `docs/SCHEMA_POLICY.md`).

